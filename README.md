# Final_NLP
Upon the execution of the dataprocessing script, a standardized and filtered dataset is generated. This refined data file subsequently serves as the direct input for executing the core modeling scripts, namely main NLP (for the baseline models) and mainBert (for the DistilBERT architecture)."
